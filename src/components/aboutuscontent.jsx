import React from 'react';

const aboutuscontent = () => {
    return <div className='bg-white text-black mt-[80px] h-screen'>
         <div className="flex flex-col justify-center items-center w-auto py-4 px-4">

           <h1 className='pb-10 font-bold text-3xl uppercase text-center'>hello</h1>
            <p> an image is not rendering properly, and you want to zoom out to see the entire image, you can use CSS styles to adjust the size and scaling of the image. Here's how you can do it:

             Adjust CSS for the Image Container: Wrap the image in a container (a div element, for example) and apply CSS styles to control its size and scaling.

            Apply CSS for the Image: Use CSS properties to control the scaling of the image inside the container.

               here's an example of how you could implement this in React with inline styles   Vite-based build setup directly in the browser, so it 
                is almost identical to the local setup but doesn't 
                require installing anything on your machine. You can 
                navigate to vite to select which framework
                 to use   You can try Vite online on StackBlitz. It runs the 
                Vite-based build setup directly in the browser, so it 
                is almost identical to the local setup but doesn't 
                require installing anything on your machine. You can 
                navigate to vite to select which framework
                 to use  You can try Vite online on StackBlitz. It runs the 
                Vite-based build setup directly in the browser, so it 
                is almost identical to the local setup but doesn't 
                require installing anything on your machine. You can 
                navigate to vite to select which framework</p>

         </div>
          
   
    </div>;
}

export default aboutuscontent;